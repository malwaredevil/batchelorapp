node_modules
dist
build
.pnpm-store
pnpm-lock.yaml
*.tsbuildinfo
lib/api-zod/src/
lib/api-client-react/src/
**/.generated/
**/*.semgrep*
**/semgrep_rules.json
.config/
# Throwaway fixture check-scheduler-names.test.ts briefly writes into this
# real source dir during integration tests. ESLint already excludes it
# (see eslint.config.mjs); Prettier needs the same exclusion or a `lint`
# run that overlaps a `test` run can transiently fail on it.
artifacts/api-server/src/_ci_sentinel_scheduler_names_.ts
# Same pattern: pii-scan.test.ts briefly writes a PII-injection fixture into
# lib/ to verify the scanner catches real emails.  Exclude it from Prettier so
# a concurrent lint+test run never trips on a transiently-present temp file.
lib/_temp_pii_scan_test_fixture_delete_me.ts
