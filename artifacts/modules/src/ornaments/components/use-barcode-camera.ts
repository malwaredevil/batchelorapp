// Kept as an adapter so existing Ornaments call sites retain their established
// import path while the scanning engine remains shared with Elaine.
export { useBarcodeCamera } from "@workspace/barcode-scanner";
