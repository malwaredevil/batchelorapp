{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build-spec": "tsx build-spec.ts",
    "codegen": "pnpm run build-spec && orval --config ./orval.config.ts && node patch-index.cjs"
  },
  "devDependencies": {
    "orval": "^8.9.1",
    "tsx": "catalog:",
    "yaml": "^2.8.4"
  }
}
